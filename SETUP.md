# Meetify Setup Guide

Пошаговое руководство по запуску проекта Meetify.

## Шаг 1: Установка необходимого ПО

### 1.1 Java Development Kit (JDK) 17+
Уже установлено (проект использует Java 17)

### 1.2 PostgreSQL
1. Скачайте PostgreSQL с [postgresql.org/download](https://www.postgresql.org/download/)
2. Установите с настройками по умолчанию
3. Запомните пароль для пользователя `postgres`

### 1.3 Node.js и npm
1. Скачайте Node.js 18+ с [nodejs.org](https://nodejs.org/)
2. Выберите LTS версию (рекомендуется)
3. Установите с настройками по умолчанию
4. Проверьте установку:
```bash
node --version
npm --version
```

## Шаг 2: Настройка базы данных

### 2.1 Создание базы данных

Откройте командную строку и выполните:

```bash
# Войти в PostgreSQL
psql -U postgres

# Ввести пароль, который установили при установке PostgreSQL

# Создать базу данных
CREATE DATABASE meetify;

# Выйти
\q
```

Или через pgAdmin (если установлен):
1. Откройте pgAdmin
2. Подключитесь к серверу PostgreSQL
3. Правый клик на "Databases" → "Create" → "Database"
4. Имя: `meetify`
5. Нажмите "Save"

### 2.2 Настройка подключения

Откройте файл `src/main/resources/application.properties` и обновите данные для подключения к БД:

```properties
spring.datasource.username=postgres
spring.datasource.password=ВАШ_ПАРОЛЬ_POSTGRES
```

## Шаг 3: Запуск Backend

### 3.1 Сборка проекта

Откройте командную строку в корневой папке проекта (`D:\Coding\pet\meetify`) и выполните:

```bash
# Windows
gradlew.bat build

# Linux/Mac
./gradlew build
```

### 3.2 Запуск сервера

```bash
# Windows
gradlew.bat bootRun

# Linux/Mac
./gradlew bootRun
```

Backend запустится на `http://localhost:8080`

Вы должны увидеть в консоли:
```
Started MeetifyApplication in X seconds
```

## Шаг 4: Запуск Frontend

### 4.1 Установка зависимостей

Откройте **новую** командную строку, перейдите в папку frontend:

```bash
cd D:\Coding\pet\meetify\frontend
npm install
```

Установка займет 1-3 минуты.

### 4.2 Запуск dev сервера

```bash
npm run dev
```

Frontend запустится на `http://localhost:3000`

Вы должны увидеть:
```
- Local:   http://localhost:3000
```

## Шаг 5: Проверка работы

### 5.1 Откройте приложение

Откройте браузер (Chrome, Edge, или Firefox) и перейдите на:
```
http://localhost:3000
```

### 5.2 Создайте комнату

1. Введите ваше имя в поле "Your Name"
2. Введите название комнаты в "Room Name"
3. Нажмите "Create Room"
4. Разрешите доступ к камере и микрофону

### 5.3 Присоединитесь к комнате с другого устройства

1. Скопируйте Room Code (например: `A1B2C3D4`)
2. Откройте `http://localhost:3000` в другой вкладке или на другом устройстве
3. Введите имя и Room Code
4. Нажмите "Join Room"

## Возможные проблемы и решения

### Проблема: Backend не запускается

**Ошибка:** `Failed to connect to database`
- **Решение:** Проверьте, что PostgreSQL запущен
- Проверьте username и password в `application.properties`
- Убедитесь, что база данных `meetify` создана

**Ошибка:** `Port 8080 is already in use`
- **Решение:** Закройте другое приложение, использующее порт 8080
- Или измените порт в `application.properties`: `server.port=8081`

### Проблема: Frontend не запускается

**Ошибка:** `npm: command not found` или `node: command not found`
- **Решение:** Node.js не установлен или не в PATH
- Переустановите Node.js
- Перезапустите командную строку

**Ошибка:** `EADDRINUSE: address already in use :::3000`
- **Решение:** Порт 3000 занят
- Закройте другое приложение на порту 3000
- Или запустите на другом порту: `npm run dev -- -p 3001`

### Проблема: Камера/микрофон не работают

- **Решение 1:** Разрешите доступ в браузере
  - Chrome: Настройки → Конфиденциальность и безопасность → Настройки сайта → Камера/Микрофон
- **Решение 2:** Убедитесь, что используете `localhost` (не IP адрес)
- **Решение 3:** Проверьте, что камера не используется другим приложением

### Проблема: WebSocket не подключается

**Ошибка в консоли браузера:** `WebSocket connection failed`
- **Решение:** Убедитесь, что backend запущен на порту 8080
- Проверьте `.env.local` в frontend:
  ```env
  NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
  NEXT_PUBLIC_WS_BASE_URL=http://localhost:8080
  ```

## Архитектура системы

```
┌─────────────────┐         ┌──────────────────┐
│   Frontend      │ HTTP    │    Backend       │
│   Next.js       │────────▶│   Spring Boot    │
│   localhost:3000│         │   localhost:8080 │
│                 │ WS      │                  │
│                 │◀───────▶│                  │
└─────────────────┘         └────────┬─────────┘
        │                            │
        │ WebRTC P2P                 │
        │                            ▼
        │                    ┌───────────────┐
        └───────────────────▶│  PostgreSQL   │
                             │   Database    │
                             └───────────────┘
```

## Порты

- **Frontend (Next.js):** 3000
- **Backend (Spring Boot):** 8080
- **PostgreSQL:** 5432
- **WebSocket:** 8080/ws

## Следующие шаги

После успешного запуска:
1. Прочитайте [README.md](README.md) для общей информации
2. Изучите [API_USAGE.md](API_USAGE.md) для деталей API
3. Просмотрите [CLAUDE.md](CLAUDE.md) для понимания архитектуры

## Готово к использованию! 🎉

Теперь вы можете:
- Создавать видеокомнаты
- Приглашать участников
- Проводить видеоконференции
- Использовать чат в реальном времени
